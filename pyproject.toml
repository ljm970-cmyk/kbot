[project]
name = "infinite-buy-bot"
version = "4.0.0"
description = "무한매수법 V4.0 자동매매 봇 (KST 고정, 프리장 30분 지연)"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.0.0",
    "httpx>=0.27.0",
    "apscheduler>=3.10.0",
]

[project.scripts]
v4bot = "src.main:main"